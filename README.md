git init - tells git to start tracking changes (from scratch) in current folder
git status
git log - shows commit history (starting at most recent)
git add .
git commit -m "commit message"
git diff
git push
git clone